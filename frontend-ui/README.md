# RetargetIQ Frontend UI


Place this folder at `RetargetIQ/frontend-ui`.


Build & run (local development):


1. cd frontend-ui
2. npm install
3. npm run dev


To build Docker image and run in compose (recommended with your existing docker-compose):


1. From repo root: docker compose build frontend-ui
2. docker compose up -d frontend-ui